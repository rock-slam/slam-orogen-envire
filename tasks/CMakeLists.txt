# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(envireTaskLib)
ADD_LIBRARY(${ENVIRE_TASKLIB_NAME} SHARED 
    ${ENVIRE_TASKLIB_SOURCES})

add_dependencies(${ENVIRE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ENVIRE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ENVIRE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ENVIRE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ENVIRE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ENVIRE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ENVIRE_TASKLIB_HEADERS} Orocos.hpp
    DESTINATION include/orocos/envire)

